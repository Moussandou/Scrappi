rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write access to all documents for the MVP
      // TODO: secure this before leaving the MVP phase
      allow read, write: if true;
    }
  }
}
